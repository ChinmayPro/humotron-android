<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- HTML: background: rgba(5,9,10,.62) -->
    <solid android:color="@color/console_profile_card_bg" />
    <!-- HTML: border: 1px solid var(-hair) (approx 8% white) -->
    <stroke android:width="1dp" android:color="@color/console_profile_card_stroke" />
    <corners android:radius="24dp" />
</shape>
